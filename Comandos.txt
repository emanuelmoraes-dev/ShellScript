# Lista de comandos para Shell Script
# Autor: Emanuel Moraes
# email: emanuelmoraes297@gmail.com

# Buscar programa pelo terminal ((p/ Ubuntu(e derivados ))

sudo apt-cache search programa | grep programa

# Buscar programa pelo terminal (( p/ fedora e derivados ))

sudo dnf search programa | grep programa

# Buscar programa pelo termin al (( Arch Linux e derivados ))

sudo pacman -Ss programa

# Instalar programa pelo terminal ((p/ Ubuntu(e derivados ))

sudo apt-get update
sudo apt-get install programa

# Instalar programa pelo terminal (( p/ fedora e derivados ))

sudo dnf install programa

# instalar programa pelo termin al (( Arch Linux e derivados ))

sudo pacman -Sy programa

# Instalar pacote deb

sudo dpkg -i pacote.deb
sudo apt-get install -f

# Instalar pacote e dependências automaticamente de um rpm

sudo dnf install --nogpgcheck pacote

# Instalar pacote tar.pkg

sudo pacman -U programa.tar.pkg

# Instalar pacote pkgbuild

makepkg -ci

# Instalar pacote AUR

git clone repositorio_do_aur
cd pasta_criada
makepkg -ci

# Instalar programa de código fonte

cat readme* | less (( ler as instruções se houver ))
./configure (( se houver ))
make
sudo make install

﻿#INSTALAR DRIVER DE WIFI PARA PLACAS RTL8723BE

lspci

sudo apt-get install linux-headers-generic build-essential git dkms (( p/ Ubuntu ))
sudo dnf install kernel-devel-$(uname -r) git (( p/ Fedora ))
sudo pacman -S base-devel git linux-headers iw rfkill wireless_tools net-tools (( p/ Arch ))
git clone http://github.com/lwfinger/rtlwifi_new.git
cd rtlwifi_new && make
sudo make install
make clean
sudo nano /etc/modprobe.d/rtl8723be.conf (( Escrever "options rtl8723be fwlps=0 swlps=0" ))

#VERIFICAR SE EXT GLOB (EX DO EXT GLOB: rm !(*.zip|*.iso)) ESTÁ ATIVADO

shopt extglob

#ATIVA EXT GLOB

shopt -s extglob

#DESLIGAR WEBCAM

sudo modprobe -r uvcvideo

#LIGAR WEBCAM

sudo modprobe uvcvideo

#MUDAR ORDEM DE GRUB

sudo gedit /etc/default/grub

sudo update-grub (( p/ Ubuntu ))

sudo grub-mkconfig -o /boot/grub/grub.cfg # (( p/ linux em geral ))

#ALTERAR SENHA ROOT

sudo passwd root

#CRIAR LANÇADOR DE APLICATIVO NO MENU

su

cd /usr/share/applications

echo \
"[Desktop Entry]
Encoding=UTF-8
Type=Application
Terminal=true
Name=Eclipse IDE
Exec=$HOME/bin/eclipse
Icon=$HOME/.icons/Eclipse.png
Categories=Development;
" >> "nome".desktop

chmod +x "nome".desktop

#MONTAR ISO

mount -o loop "sua_Imagem.iso" /media/diretorio_ISO

#PERMITIR ACESSO ROOT AO VLC

sudo sed -i 's/geteuid/getppid/' /usr/bin/vlc

#REMOVER CONTA DE CONVIDADO

sudo nano /usr/share/lightdm/lightdm.conf.d/50-ubuntu.conf (( Escrever "allow-guest=false" ))

#INSTALAR GRUB (( comandos em um live cd linux ))

sudo fdisk -l
sudo mount -t ext4 `dispositivo` /mnt
sudo grub-install --root-directory=/mnt `dispositivo`

sudo update-grub (( p/ Ubuntu ))

sudo grub-mkconfig -o /boot/grub/grub.cfg # (( p/ linux em geral ))

# Dar permissão para usuário usar a impressora

sudo gpasswd -a seu-usuario lp
sudo gpasswd -a seu-usuario sys
sudo gpasswd -a seu-usuario scanner

# Instalar driver hp (( p/ Ubuntu e derivados ))

sudo apt-get install cups python-imaging ghostscript hplip
sudo apt-get install cups-filters ghostscript gsfonts
sudo apt-get install simple-scan python-pillow

sudo /etc/init.d/cups start

# Instalar driver hp (( p/ Arch Linux ))

sudo pacman -S cups python-imaging ghostscript hplip
sudo pacman -S cups-filters ghostscript gsfonts
sudo pacman -S simple-scan python-pillow

systemctl enable org.cups.cupsd.service
systemctl daemon-reload
systemctl start org.cups.cupsd.service

sudo hp-setup -i

# Instalar impressora hp

hp-setup (( Se nao funcionar tente instalar modo texto com o comando "hp-setup -i" ))

# Instalar pacotes no arch linux

makepkg -cis

# Instalar chave pública

gpg --recv-keys "chave"

# Arch inserir linguagens

nano /etc/locale.gen (( Descomente a linguagem desejada. Recomenda-se fortemente deixar tambem o ingles ))
sudo locale-gen

# Subistututir texto em multiplos arquivos

find "pasta" -iname "arquivo" -type f -print | xargs sed -i 's/texto1/texto2/g'

# Subistututir texto em multiplos arquivos com confirmação

find ./ -name "arquivo" -exec vim '+%s/texto/textoDaSubistituicao/gc' {} \;

# Subistututir texto em multiplos arquivos com confirmação sem recursao

find ./ -maxdepth 1 -name "arquivo" -exec vim '+%s/texto/textoDaSubistituicao/gc' {} \;x

# Busca as linhas dos arquivos que comtem uma string especificada


find ./ -type f -name "arquivo" -exec grep -l -i "customUserDetailsService" {} \;


# Alterar ação do fechamento da tampa (( Gnome ))

sudo gedit /etc/systemd/logind.conf (( procurar/inserir o texto "HandleLidSwitch='ops'" ops: "ignore" "poweroff" "reboot" "suspend" "hibernate" "lock" ))

systemctl restart systemd-logind

# Encerrar Sessão (( Gnome ))

gnome-session-quit

# Encerrar Sessão

sudo pkill -9 -u username

# Conectar a cabo de internet 

ifconfig -a
dhcpcd <interface> # OU
dhclient <interface>

# Conectar a cabo de internet Arch Linux

Systemctl start dhcpcd

# Conectar a wifi

iwconfig

iwlist scan (( se nao funcionar antes digite ifup <interface> ctrl + c ))

1. Rede aberta (OPN)

iwconfig <interface> essid <nome da rede>
dhclient <interface>

2. Rede encriptada (WEP)

iwconfig <interface> mode managed key <senha>
iwconfig <interface> essid <nome da rede>
dhclient <interface>

3. Rede encriptada 2 - A Vingança :) (WPA/WPA2)

wpa_passphrase <nome da rede> > /caminho/do/arquivo.conf
wpa_supplicant -Dwext -i<interface> -c /caminho/do/arquivo.conf (( se der certo de ctrl + c ))
wpa_supplicant -Dwext -i<interface> -c /caminho/do/arquivo.conf -B

dhclient -r
dhclient <interface>

# Command Create Binary by file
od -An -vtx1 Check.tar > Check.txt

# Command Create file by binary
LC_ALL=C tr -cd 0-9a-fA-F < Check.txt | xxd -r -p > Check.tar

# Create Live Usb Bootable
sudo dd if=/path/to/image.iso of=/dev/sdX bs=8M status=progress oflag=direct

# Criar ISO de arquivos
mkisofs -o /tmp/cd.iso /tmp/directory/

# Criar ISO de CD/DVD
dd if=/dev/cdrom of=~/cdrom_image.iso

# Reinstalar drivers de som (( p/ Ubuntu ))

sudo apt-get remove alsa-utils
sudo apt-get remove alsamixer
sudo apt-get purge pulseaudio
sudo apt-get clean
sudo apt-get autoremove

(( Reinicia o pc ))

(( Depois de reiniciar ))

rm -r ~/.pulse ~/.asound* ~/.pulse-cookie ~/.config/pulse

sudo apt-get install alsa-utils
sudo apt-get install alsamixer
sudo apt-get install pulseaudio
sudo alsa force-reload pavucontrol

# Descompactar arquivos compactados

## tar.gz

tar -xvzf arquivo.tar.gz -C path_destino

## tar.bz2

tar -xvjf arquivo.tar.bz2 -C path_destino

## tar.xz (( precisa do pacote xz-utils ))

tar -xvJf arquivo.tar.xz -C path_destino

## zip

unzip arquivo.zip -d path_destino

## rar

unrar e arquivo.rar path_destino

(( or ))

unrar x arquivo.rar

# Compactar arquivos

## tar.gz

tar -cvf arquivo.tar arquivo_a_compactar
gzip -9 arquivo.tar

(( or ))

tar -cvzf arquivo.tar.gz arquivo_a_compactar

## tar.bz2

tar -cvf arquivo.tar arquivo_a_compactar
bzip2 arquivo.tar

(( or ))

tar -cvjf arquivo.tar.bz2 arquivo_a_compactar

## tar.xz

tar -cvJf arquivo.tar.xz arquivo

## zip

zip arquivo.zip arquivo_a_compactar

(( or ))

zip -r arquivo.zip pasta_a_compactar

## rar

rar a arquivo.rar arquivo_ou_pasta_a_compactar

# Desativar temporariamente um dispositivo de entrada

xinput list (( lista os dispositivos de entrada ))

xinput float <id> (( desativa temporariamente o dispositivo com o id informado ))

xinput reattach <id> <slave keyboard> (( ativa o dispositivo com o id informado e com o numero slave keybord informado ))

# Desativar permanentemente um dispositivo de entrada

sudo cp /etc/default/grub /etc/default/grub.original (( Cria um arquivo de backup para reverter o processo ))

sudo <editor> /etc/default/grub (( Localize a linua 'GRUB_CMDLINE_LINUX_DEFAULT="quiet splash"' e substitua por 'GRUB_CMDLINE_LINUX_DEFAULT="quiet splash i8042.nokbd"' ))

sudo update-grub

# Reverter o procedimento anterior

sudo cp /etc/default/grub.original /etc/default/grub

sudo update-grub

# Mudar verção padrão do java

sudo gedit /etc/profile 

(( 
Escrever/Mudar linhas: 
export JAVA_HOME="diretorio_java"
export PATH="$PATH:diretorio_java/bin"
))

# Mudar verção padrão do java (( p/ Fedora ))

alternatives --config java
alternatives ---config javac

# Listar portas usadas no linux

netstat -antp

# Fazer o flex e bison funcionar

sudo apt install flex bison

sudo apt install libfl2 libfl-dev